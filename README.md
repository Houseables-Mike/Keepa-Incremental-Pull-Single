This is a simple example of how to properly pull data via Keepa's API using ASIN codes as the target.
 - The Keepa API key that will be used should be stored into an environment variable for the scrpit to access, using the dot_env python package.
 - Hard code a test ASIN code into the script for the parameter "TEST_ASIN" which will be used for the test pull.
